{
  "name": "scaledash",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "postinstall": "cd ./packages/api && yarn install && cd ../web && yarn install",
    "dev:api": "cd ./packages/api && yarn dev",
    "dev:web": "cd ./packages/web && yarn dev"
  },
  "devDependencies": {
    "tslint": "^5.11.0",
    "typescript": "^3.2.1"
  }
}
